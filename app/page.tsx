'use client';

import { useEffect, useMemo, useState } from 'react';
import { asianQuestions, coffeeQuestions, lemonadeQuestions, russianQuestions } from './question-banks';
import { advancedQuestions } from './advanced-question-banks';
import { expiryItems, expiryQuestions, type ExpiryLabelMode } from './expiry-data';

type VisualCheck = { label: string; title: string; fields: { label: string; value: string }[]; helper?: string };
type Question = { id: number; text: string; options: string[]; correct: number; note: string; image?: string; imageAlt?: string; visualCheck?: VisualCheck };
type Category = { id: string; title: string; subtitle: string; icon: string; color: string; questions?: Question[]; passScore?: number };
type Attempt = { id: number; employeeName: string; categoryId: string; categoryTitle: string; score: number; total: number; passed: boolean; completedAt: string; answers: number[] | null };
type TeamMember = { id: number; name: string; pinConfigured: boolean; createdAt: string };
type StaffMember = { id: number; name: string };
type IssuedPin = { id: number; name: string; pin: string };
type ManagerTab = 'summary' | 'attempts' | 'team' | 'deadlines';
type ProgressStatus = 'passed' | 'failed' | 'missing';
type TrainingLevel = 'basic' | 'advanced';

const fryerQuestions: Question[] = [
  { id: 1, text: 'Какая рабочая температура фритюра установлена для новых стандартов?', options: ['175°C ± 5°C', '180°C ± 5°C', '185°C', '190°C ± 5°C'], correct: 1, note: 'Для всех новых стандартов фритюра рабочая температура — 180°C ± 5°C.' },
  { id: 2, text: 'До какого уровня можно заполнять корзину фритюра?', options: ['Полностью', 'Не более чем на 3/4', 'Не более чем наполовину', 'Ограничений нет'], correct: 2, note: 'Корзину заполняют не более чем наполовину: так масло не переливается и продукт готовится равномерно.' },
  { id: 3, text: 'Когда нужно встряхнуть корзину, если для продукта нет особого правила?', options: ['Через 30 секунд', 'Через 1 минуту', 'В конце приготовления', 'Не нужно встряхивать'], correct: 0, note: 'Общее правило — встряхнуть корзину через 30 секунд, если в стандарте продукта не указано иначе.' },
  { id: 4, text: 'Сколько времени продукт должен стечь после жарки?', options: ['5–7 секунд', '10–15 секунд', '20–30 секунд', 'Сразу переложить в упаковку'], correct: 1, note: 'После жарки дайте маслу стечь 10–15 секунд перед перекладыванием.' },
  { id: 5, text: 'Как часто нужно оценивать состояние масла?', options: ['В конце каждой смены', 'Раз в неделю', 'После процеживания, не реже одного раза в 2 дня', 'Только когда масло потемнело'], correct: 2, note: 'Состояние масла оценивают после процеживания, но не реже одного раза в 2 дня.' },
  { id: 6, text: 'Как правильно посолить картофель фри, батат или картофель по-деревенски?', options: ['Посолить до жарки', 'После стекания масла переложить в гастроёмкость 1/6 и посолить', 'Посолить прямо в корзине', 'Соль не использовать'], correct: 1, note: 'Соль добавляют после стекания масла: продукт перекладывают в гастроёмкость 1/6.' },
  { id: 7, text: 'Какое правило действует для хашбраунов?', options: ['Не более 4 штук, строго в один слой', 'Не более 6 штук, можно в два слоя', 'Не более 2 штук, в один слой', 'Количество и слой не важны'], correct: 0, note: 'Хашбрауны готовят не более 4 штук и строго в один слой.' },
  { id: 8, text: 'Что нужно сделать, если из палочек с сыром начал вытекать сыр?', options: ['Дожарить до конца', 'Сразу достать', 'Увеличить температуру', 'Встряхнуть корзину'], correct: 1, note: 'Если сыр начал вытекать, палочки нужно сразу достать, чтобы не потерять начинку.' },
  { id: 9, text: 'Сколько готовится картофель фри во фритюре?', options: ['2:30', '3:00', '3:30', '4:00'], correct: 1, note: 'Картофель фри готовится 3:00.' },
  { id: 10, text: 'Какая фасовка картофеля фри S / M / L?', options: ['75 / 150 / 225 г', '77 / 154 / 230 г', '90 / 150 / 225 г', '70 / 140 / 210 г'], correct: 0, note: 'Фасовка картофеля фри: S — 75 г, M — 150 г, L — 225 г.' },
  { id: 11, text: 'Какой стандарт у картофеля по-деревенски?', options: ['3:00; S / M / L: 75 / 150 / 225 г', '3:30; S / M / L: 77 / 154 / 230 г', '3:30; S / M: 90 / 150 г', '2:30; S / M / L: 77 / 154 / 230 г'], correct: 1, note: 'Картофель по-деревенски: 3:30; S / M / L — 77 / 154 / 230 г.' },
  { id: 12, text: 'Какая фасовка батата фри S / M?', options: ['75 / 150 г', '77 / 154 г', '90 / 150 г', '90 / 180 г'], correct: 2, note: 'Батат фри: S — 90 г, M — 150 г.' },
  { id: 13, text: 'Какой стандарт у хашбраунов?', options: ['3:00; 2 штуки', '3:30; 2 штуки', '3:30; 4 штуки', '2:30; 2 штуки'], correct: 1, note: 'Хашбрауны готовятся 3:30; одна порция — 2 штуки.' },
  { id: 14, text: 'Какой стандарт у палочек с сыром?', options: ['2:30; S / M / L: 3 / 5 / 8 штук', '3:00; S / M / L: 3 / 5 / 8 штук', '2:30; S / M / L: 4 / 6 / 9 штук', '3:30; S / M / L: 3 / 5 / 8 штук'], correct: 0, note: 'Палочки с сыром: 2:30; S / M / L — 3 / 5 / 8 штук.' },
  { id: 15, text: 'Как готовят драники?', options: ['СВЧ 0:30, кнопка 3, в контейнере с крышкой; вручную в корзину; фритюр 1:30', 'СВЧ 1:00, кнопка 5; фритюр 3:30', 'Сразу фритюр 3:30', 'СВЧ 0:30, кнопка 3; затем гриль 1:30'], correct: 0, note: 'Драники: СВЧ 0:30, кнопка 3, в контейнере с крышкой; затем вручную в корзину и фритюр 1:30.' },
  { id: 16, text: 'Какой стандарт у жареных шампиньонов ПФ?', options: ['1:00; 30 г', '1:30; 30 г', '1:00; 50 г', '3:00; 30 г'], correct: 0, note: 'Жареные шампиньоны ПФ: 1:00; порция 30 г.' },
  { id: 17, text: 'Какой стандарт у чуррос?', options: ['3:00; 5 штук; после жарки дать маслу стечь 10–15 секунд', '2:30; 5 штук; сразу упаковать', '3:30; 4 штуки; встряхнуть через 30 секунд', '3:00; 8 штук; посолить'], correct: 0, note: 'Чуррос: 3:00; 5 штук. После жарки маслу нужно стечь 10–15 секунд.' },
  { id: 18, text: 'Какой стандарт у сырной котлеты?', options: ['2:30; 1 штука', '3:00; 1 штука; после жарки дать маслу стечь 10–15 секунд', '3:00; 2 штуки', '3:30; 1 штука'], correct: 1, note: 'Сырная котлета: 3:00; 1 штука; после жарки — стекание масла 10–15 секунд.' },
  { id: 19, text: 'Какой стандарт у куриных наггетсов?', options: ['3:00; S / M / L: 4 / 6 / 9 штук', '3:30; S / M / L: 3 / 6 / 9 штук', '3:00; S / M / L: 3 / 5 / 8 штук', '2:30; S / M / L: 4 / 6 / 9 штук'], correct: 0, note: 'Куриные наггетсы: 3:00; S / M / L — 4 / 6 / 9 штук.' },
  { id: 20, text: 'Какой стандарт у куриных стрипсов?', options: ['3:00; S / M / L: 3 / 6 / 9 штук', '3:30; S / M / L: 3 / 6 / 9 штук', '3:30; S / M / L: 3 / 5 / 8 штук', '3:00; S / M / L: 4 / 6 / 9 штук'], correct: 1, note: 'Куриные стрипсы: 3:30; S / M / L — 3 / 6 / 9 штук.' },
  { id: 21, text: 'Какая фасовка крыльев S / M / L?', options: ['3 / 6 / 9 штук', '4 / 7 / 10 штук', '3 / 5 / 8 штук', '4 / 6 / 9 штук'], correct: 2, note: 'Крылья: S / M / L — 3 / 5 / 8 штук.' },
  { id: 22, text: 'Как правильно приготовить куриные крылья?', options: ['СВЧ 1:00, кнопка 5; вручную переложить в корзину; фритюр 3:30', 'СВЧ 0:30, кнопка 3; фритюр 1:30', 'Сразу фритюр 3:30', 'СВЧ 1:00, кнопка 5; фритюр 3:00'], correct: 0, note: 'Крылья: СВЧ 1:00, кнопка 5; вручную переложить в корзину; затем фритюр 3:30.' },
  { id: 23, text: 'Какой стандарт у митболлов?', options: ['3:00; S / M / L: 4 / 7 / 10 штук', '3:30; S / M / L: 4 / 7 / 10 штук', '3:30; S / M / L: 3 / 5 / 8 штук', '3:00; S / M / L: 3 / 6 / 9 штук'], correct: 1, note: 'Митболлы: 3:30; S / M / L — 4 / 7 / 10 штук.' },
  { id: 24, text: 'Какой стандарт у рыбных палочек?', options: ['3:00; S / M / L: 3 / 5 / 8 штук; строго в один слой', '3:30; S / M / L: 3 / 5 / 8 штук; встряхнуть через 30 секунд', '3:00; S / M / L: 4 / 6 / 9 штук', '2:30; S / M / L: 3 / 5 / 8 штук'], correct: 0, note: 'Рыбные палочки: 3:00; S / M / L — 3 / 5 / 8 штук; готовить строго в один слой.' },
  { id: 25, text: 'Какой стандарт у темпурных креветок?', options: ['3:00; S / M / L: 3 / 5 / 8 штук', '3:30; S / M / L: 3 / 5 / 8 штук', '3:30; S / M / L: 4 / 7 / 10 штук', '3:00; S / M / L: 3 / 6 / 9 штук'], correct: 1, note: 'Темпурные креветки: 3:30; S / M / L — 3 / 5 / 8 штук.' },
  { id: 26, text: 'Визуальная проверка: на карточке поста для жареных пельменей есть один неверный параметр. Что нужно исправить?', options: ['Температуру фритюра: установить 180°C ± 5°C', 'Вес порции: изменить с 175 г', 'Время жарки: изменить с 3:00', 'Перед жаркой не нужно разделять слипшиеся пельмени'], correct: 0, note: 'Неверна температура: во всех новых стандартах фритюра — 180°C ± 5°C. Пельмени: 3:00 и 175 г; перед жаркой их обязательно разделяют.', image: '/training-assets/fryer-pelmeni.png', imageAlt: 'Жареные пельмени по стандарту', visualCheck: { label: 'НАЙДИТЕ ОШИБКУ', title: 'Жареные пельмени · чек поста', fields: [{ label: 'Температура', value: '175°C ± 5°C' }, { label: 'Порция', value: '175 г' }, { label: 'Таймер', value: '03:00' }], helper: 'Сверьте параметры с новым стандартом.' } },
];

const burgerQuestions: Question[] = [
  { id: 1, text: 'Как правильно разогреть говяжью котлету для актуальных стандартов бургеров?', options: ['В СВЧ 1:30, кнопка 6, в пластиковой гастроёмкости 1/6 с крышкой', 'В СВЧ 1:00, кнопка 5, без крышки', 'Во фритюре 3:00', 'В прижимном гриле 1:30'], correct: 0, note: 'Говяжью котлету разогревают в пластиковой гастроёмкости 1/6 с крышкой: СВЧ 1:30, кнопка 6.' },
  { id: 2, text: 'Визуальная проверка: на карточке подготовки булочки указан один неверный пункт. Что нужно исправить?', options: ['Развернуть булочку срезами наружу и сложить половинки одну на другую', 'Положить половинки срезами внутрь', 'Разогреть булочку целиком', 'Сначала смазать булочку соусом'], correct: 0, note: 'Булочку разворачивают срезами наружу и складывают половинки одну на другую — именно так она прогревается в прижимном гриле.', image: '/training-assets/burger-bun-warming.png', imageAlt: 'Подготовка булочки к разогреву', visualCheck: { label: 'НАЙДИТЕ ОШИБКУ', title: 'Булочка · подготовка к грилю', fields: [{ label: 'Положение срезов', value: 'Внутрь' }, { label: 'Половинки', value: 'Одна на другой' }, { label: 'Гриль', value: '01:00' }], helper: 'Проверьте положение булочки перед разогревом.' } },
  { id: 3, text: 'Сколько прогревается булочка в прижимном гриле?', options: ['0:30', '1:00', '1:30', '3:00'], correct: 1, note: 'Булочка прогревается в прижимном гриле 1:00.' },
  { id: 4, text: 'В каком направлении собирают любой бургер по актуальным схемам?', options: ['Сверху вниз', 'Снизу вверх', 'Порядок не важен', 'Сначала все соусы, затем остальные слои'], correct: 1, note: 'Бургер всегда собирают строго снизу вверх, по схеме конкретного продукта.' },
  { id: 5, text: 'Сколько котлет используется в бургере «С говяжьей и сырной котлетой»?', options: ['Одна говяжья котлета', 'Одна сырная котлета', 'По одной говяжьей и сырной котлете', 'Четыре куриных наггетса'], correct: 2, note: 'В бургере используются две котлеты: одна говяжья и одна сырная.' },
  { id: 6, text: 'Какой первый слой после нижней булочки в бургере «С говяжьей и сырной котлетой»?', options: ['Брусничный соус 14 г', 'Сырный соус 20 г витками', 'Салат айсберг 15 г', 'Котлета из говядины'], correct: 1, note: 'После нижней булочки наносят сырный соус — 20 г витками.' },
  { id: 7, text: 'Сколько брусничного соуса наносят между говяжьей и сырной котлетой?', options: ['10 г', '14 г', '20 г', '30 г'], correct: 1, note: 'После говяжьей котлеты наносят 14 г брусничного соуса витками.' },
  { id: 8, text: 'Какая масса томатов в бургере «С говяжьей и сырной котлетой»?', options: ['18 г', '20 г', '30 г', '40 г'], correct: 2, note: 'В этом бургере томаты — 30 г.' },
  { id: 9, text: 'Сколько огурцов маринованных в бургере «Гриль с сыром»?', options: ['6 г', '15 г', '20 г', '30 г'], correct: 2, note: 'В «Гриль с сыром» добавляют 20 г маринованных огурцов.' },
  { id: 10, text: 'Какая масса томатов в бургере «Гриль с сыром»?', options: ['15 г', '18 г', '30 г', '40 г'], correct: 1, note: 'Томаты в бургере «Гриль с сыром» — 18 г.' },
  { id: 11, text: 'Какой соус и в каком количестве наносится первым в авторском бургере с брусничным соусом?', options: ['Чесночный соус 10 г', 'Грибной соус 10 г', 'Брусничный соус 14 г', 'Соус гриль 14 г'], correct: 1, note: 'Первый слой после нижней булочки — грибной соус, 10 г витками.' },
  { id: 12, text: 'Сколько маринованных огурцов в авторском бургере с брусничным соусом?', options: ['6 г', '10 г', '15 г', '20 г'], correct: 0, note: 'В авторском бургере маринованные огурцы — 6 г.' },
  { id: 13, text: 'Как наносят брусничный соус в бургере «С сырной котлетой»?', options: ['Только на нижнюю булочку, 20 г', 'Только перед верхней булочкой, 20 г', 'Витками снизу и сверху, по 20 г', 'Между котлетами, 14 г'], correct: 2, note: 'В бургере «С сырной котлетой» брусничный соус наносят витками снизу и сверху — по 20 г.' },
  { id: 14, text: 'Какая масса томатов в бургере «С сырной котлетой»?', options: ['18 г', '30 г', '40 г', '50 г'], correct: 2, note: 'Томаты в бургере «С сырной котлетой» — 40 г.' },
  { id: 15, text: 'Какой соус наносят в бургере «С сырной котлетой» и сколько?', options: ['Брусничный соус по 20 г витками снизу и сверху', 'Сырный соус 20 г только снизу', 'Чесночный соус по 10 г', 'Соус гриль по 14 г'], correct: 0, note: 'В бургере «С сырной котлетой» брусничный соус наносят по 20 г витками снизу и сверху.' },
  { id: 16, text: 'Сколько наггетсов кладут в чикенбургер?', options: ['2 штуки', '3 штуки', '4 штуки', '5 штук'], correct: 2, note: 'В чикенбургере используется 4 куриных наггетса.' },
  { id: 17, text: 'Какой соус и сколько используют в чикенбургере?', options: ['Соус чили сладкий 14 г', 'Чесночный соус по 8 г витками снизу и сверху', 'Соус тартар по 15 г', 'Грибной соус 10 г'], correct: 1, note: 'В чикенбургере чесночный соус наносят по 8 г витками снизу и сверху.' },
  { id: 18, text: 'Как готовят бургер «С хрустящей курицей»?', options: ['Сливочный сыр 16 г, 2 стрипса, соус чили сладкий 14 г', 'Чесночный соус 8 г, 4 наггетса', 'Тартар 15 г, 2 рыбные палочки', 'Брусничный соус 20 г, сырная котлета'], correct: 0, note: 'В бургере «С хрустящей курицей»: сливочный сыр 16 г, 2 стрипса, соус чили сладкий 14 г.' },
  { id: 19, text: 'Какой стандарт у фишбургера?', options: ['2 рыбные палочки, салат айсберг 20 г, тартар по 15 г витками снизу и сверху', '4 наггетса и чесночный соус по 8 г', '2 стрипса и соус чили 14 г', 'Сырная котлета и брусничный соус по 20 г'], correct: 0, note: 'Фишбургер: 2 рыбные палочки, салат айсберг 20 г, тартар по 15 г витками снизу и сверху.' },
  { id: 20, text: 'Какой белковый компонент используют в бургере «Кимчи с говяжьей котлетой»?', options: ['2 стрипса из фритюра', 'Говяжью котлету, разогретую в СВЧ', '4 куриных наггетса', '2 рыбные палочки'], correct: 1, note: 'В «Кимчи с говяжьей котлетой» используют говяжью котлету, разогретую в СВЧ.' },
  { id: 21, text: 'Сколько капусты кимчи кладут в каждый из двух слоёв бургера «Кимчи с говяжьей котлетой»?', options: ['10 г', '15 г', '20 г', '30 г'], correct: 2, note: 'Капусту кимчи кладут в два слоя, по 20 г в каждом.' },
  { id: 22, text: 'Какая масса сыра чеддер в сезонных бургерах с кимчи?', options: ['1 слайс, 12 г', '2 слайса, 24 г', '2 слайса, 40 г', '3 слайса, 24 г'], correct: 1, note: 'В обоих бургерах с кимчи — 2 слайса чеддера, всего 24 г.' },
  { id: 23, text: 'Какой белковый компонент используют в бургере «Кимчи со стрипсами»?', options: ['2 стрипса, приготовленные во фритюре 3:00', '4 куриных наггетса', 'Сырную котлету', '2 рыбные палочки'], correct: 0, note: 'В «Кимчи со стрипсами» используют 2 стрипса; их готовят во фритюре 3:00.' },
  { id: 24, text: 'Как готовится хэшбраун для бургера «С грибами а-ля рус»?', options: ['СВЧ 1:30, кнопка 6', 'Фритюр 3:00', 'Прижимной гриль 1:00', 'Фритюр 1:30'], correct: 1, note: 'Хэшбраун для бургера «С грибами а-ля рус» готовится во фритюре 3:00.' },
  { id: 25, text: 'Сколько жареных шампиньонов добавляют в бургер «С грибами а-ля рус»?', options: ['15 г', '20 г', '23 г', '30 г'], correct: 2, note: 'В бургер «С грибами а-ля рус» добавляют 23 г жареных шампиньонов.' },
  { id: 26, text: 'Какой слой идёт непосредственно перед верхней булочкой в бургере «С грибами а-ля рус»?', options: ['Жареный лук 5 г', 'Маринованные огурцы 30 г', 'Чесночный соус 10 г витками', 'Сыр чеддер 12 г'], correct: 2, note: 'Последний слой перед верхней булочкой — чесночный соус, 10 г витками.' },
];

const pizzaQuestions: Question[] = [
  { id: 1, text: 'Визуальная проверка: на карточке подготовки римской пиццы указан один неверный шаг. Что нужно исправить?', options: ['Переложить основу на чистую поверхность и собирать пиццу на весах', 'Сразу поставить основу в печь', 'Собирать на подносе без весов', 'Собирать пиццу в коробке'], correct: 0, note: 'Основу кладут на чистую поверхность, а пиццу собирают на весах. На подносе без весов собирать нельзя.', image: '/training-assets/pizza-base.png', imageAlt: 'Основа римской пиццы', visualCheck: { label: 'НАЙДИТЕ ОШИБКУ', title: 'Римская пицца · подготовка', fields: [{ label: 'Поверхность', value: 'Поднос' }, { label: 'Контроль веса', value: 'Не используется' }, { label: 'Печь после сборки', value: 'Режим «Пицца» · 06:00' }], helper: 'Сверьте подготовку основы до начала сборки.' } },
  { id: 2, text: 'Какой режим и время выпекания римской пиццы?', options: ['Пароконвектомат, режим «Пицца» — 06:00', 'Гриль — 01:00', 'Фритюр — 03:00', 'СВЧ, кнопка 6 — 01:30'], correct: 0, note: 'Все римские пиццы выпекают в пароконвектомате: режим «Пицца», 06:00.' },
  { id: 3, text: 'Что нужно сделать с пиццей после печи?', options: ['Переложить в коробку, нарезать на 6 равных кусочков, затем добавить компоненты «после печи»', 'Сразу закрыть коробку', 'Посолить и выдать без нарезки', 'Нарезать на 8 кусочков до печи'], correct: 0, note: 'После печи: коробка → 6 равных кусочков → компоненты «после печи».' },
  { id: 4, text: 'Как распределяют ингредиенты по пицце?', options: ['Равномерно по всей поверхности', 'Только по центру', 'Вдоль одного края', 'В произвольном порядке и месте'], correct: 0, note: 'Все ингредиенты распределяют равномерно по всей поверхности. Исключение — ветчина.' },
  { id: 5, text: 'Как выкладывают ветчину на пицце?', options: ['Полосами 2–4–4–4–2', 'Кругом по 5 слайсов', 'Только в центр, 8 слайсов', 'В четыре ряда по 4 слайса'], correct: 0, note: 'Ветчину выкладывают полосами по схеме 2–4–4–4–2.' },
  { id: 6, text: 'Какой базовый соус у пиццы «Карбонара»?', options: ['Чесночный соус 60 г витками', 'Томатный соус 60 г витками', 'Тартар 60 г витками', 'Сырный соус 25 г'], correct: 0, note: 'Для «Карбонары» первым после коржа наносят чесночный соус — 60 г витками.' },
  { id: 7, text: 'Сколько тёртой моцареллы в пицце «Карбонара»?', options: ['60 г', '75 г', '115 г', '150 г'], correct: 1, note: 'В «Карбонаре» используется 75 г тёртой моцареллы.' },
  { id: 8, text: 'Какой слой сырного соуса в пицце «Карбонара»?', options: ['25 г диагональными линиями до печи', '20 г, 12 витков после печи', '60 г витками', 'Соус не используется'], correct: 0, note: 'Сырный соус в «Карбонаре» — 25 г диагональными линиями, он наносится до печи.' },
  { id: 9, text: 'Сколько жареного бекона ПФ в пицце «Карбонара»?', options: ['20 г', '25 г', '30 г', '35 г'], correct: 3, note: 'В «Карбонаре» — 35 г жареного бекона ПФ.' },
  { id: 10, text: 'Какая масса красного лука ПФ в пицце «Карбонара»?', options: ['10 г', '15 г', '20 г', '30 г'], correct: 1, note: 'Красный лук ПФ в «Карбонаре» — 15 г.' },
  { id: 11, text: 'Когда и сколько прованских трав добавляют в пиццу «Карбонара»?', options: ['После печи, 1 г', 'До печи, 10 г', 'После печи, 15 г', 'Травы не используются'], correct: 0, note: 'Прованские травы добавляют после печи; масса — 1 г.' },
  { id: 12, text: 'Как поступает пицца «Пепперони» на сборку?', options: ['Уже собранной; её нужно только выпечь 06:00', 'Собирается из томатного соуса и 75 г моцареллы', 'Поступает без соуса', 'Готовится во фритюре'], correct: 0, note: '«Пепперони» поступает уже собранной; её выпекают в режиме «Пицца» 06:00.' },
  { id: 13, text: 'Сколько томатного соуса в пицце «Маргарита»?', options: ['45 г', '60 г', '75 г', '80 г'], correct: 1, note: 'В «Маргарите» томатный соус — 60 г витками.' },
  { id: 14, text: 'Сколько моцареллы в пицце «Маргарита»?', options: ['75 г', '90 г', '115 г', '120 г'], correct: 2, note: 'В «Маргарите» используется 115 г сыра моцарелла.' },
  { id: 15, text: 'Как завершают пиццу «Маргарита» после печи?', options: ['Добавляют прованские травы 1 г', 'Наносят чесночный соус 20 г', 'Добавляют рукколу 20 г', 'Наносят соус для шаурмы 25 г'], correct: 0, note: 'После печи на «Маргариту» добавляют прованские травы — 1 г.' },
  { id: 16, text: 'Какой набор ингредиентов до печи у пиццы «Чесночный цыплёнок»?', options: ['Чесночный соус 60 г, моцарелла 75 г, сушёный чеснок 1 г, курица 60 г, томаты 30 г', 'Томатный соус 60 г, бекон 35 г, лук 15 г', 'Тартар 60 г, форель 50 г, руккола 20 г', 'Томатный соус 60 г, говядина 44 г, огурцы 30 г'], correct: 0, note: 'До печи: чесночный соус 60 г, моцарелла 75 г, чеснок 1 г, курица 60 г, томаты 30 г.' },
  { id: 17, text: 'Какой соус наносят после печи на «Чесночный цыплёнок»?', options: ['Чесночный соус 20 г, 12 витков', 'Сырный соус 25 г диагональными линиями', 'Тартар 60 г витками', 'Соус для шаурмы 25 г'], correct: 0, note: 'После печи наносят чесночный соус — 20 г, 12 витков.' },
  { id: 18, text: 'Какой базовый соус у пиццы «С лососем»?', options: ['Томатный соус 60 г', 'Чесночный соус 60 г', 'Соус тартар ПФ 60 г витками', 'Сырный соус 25 г'], correct: 2, note: 'Для пиццы «С лососем» используют соус тартар ПФ — 60 г витками.' },
  { id: 19, text: 'Какой набор добавляют после печи на пиццу «С лососем»?', options: ['Руккола 20 г, черри 60 г, форель 50 г, тартар 20 г', 'Бекон 35 г, красный лук 15 г, травы 1 г', 'Курица 60 г, томаты 30 г, чесночный соус 20 г', 'Говядина 44 г, огурцы 30 г, соус для шаурмы 25 г'], correct: 0, note: 'После печи: руккола 20 г, черри 60 г, форель 50 г и тартар 20 г.' },
  { id: 20, text: 'Как нужно подготовить форель для пиццы «С лососем»?', options: ['Порвать руками; масса 50 г', 'Нарезать кубиком; масса 30 г', 'Выложить 16 слайсов', 'Обжарить во фритюре 3:00'], correct: 0, note: 'Форель слабосолёную используют 50 г и рвут руками.' },
  { id: 21, text: 'Как поступает на сборку пицца «4 сыра»?', options: ['Уже собранной; её нужно только выпечь 06:00', 'Собирается с 115 г моцареллы', 'Поступает с соусом тартар', 'Требует добавить травы после печи'], correct: 0, note: 'Пицца «4 сыра» поступает собранной; её выпекают 06:00.' },
  { id: 22, text: 'Сколько котлеты говяжьей кубиком в пицце «Бургер»?', options: ['30 г', '40 г', '44 г', '50 г'], correct: 2, note: 'В пицце «Бургер» используют 44 г говяжьей котлеты кубиком.' },
  { id: 23, text: 'Сколько маринованных огурцов ПФ в пицце «Бургер»?', options: ['15 г', '20 г', '25 г', '30 г'], correct: 3, note: 'Маринованные огурцы ПФ в пицце «Бургер» — 30 г.' },
  { id: 24, text: 'Какой компонент наносят после печи на пиццу «Бургер»?', options: ['Соус для шаурмы ПФ 25 г диагональными линиями', 'Сырный соус 25 г до печи', 'Прованские травы 1 г', 'Чесночный соус 20 г, 12 витков'], correct: 0, note: 'После печи на пиццу «Бургер» наносят 25 г соуса для шаурмы ПФ диагональными линиями.' },
  { id: 25, text: 'Какой стандарт у пиццы «Ветчина-грибы»?', options: ['Ветчина 16 слайсов, шампиньоны ПФ 25 г, грибной соус ПФ 25 г после печи', 'Бекон 35 г, лук 15 г, сырный соус 25 г', 'Курица 60 г, томаты 30 г, чесночный соус 20 г после печи', 'Форель 50 г, черри 60 г, тартар 20 г после печи'], correct: 0, note: '«Ветчина-грибы»: ветчина 16 слайсов, шампиньоны 25 г; после печи — грибной соус 25 г.' },
  { id: 26, text: 'Какой состав мясной начинки у пиццы «Мясная»?', options: ['Ветчина 16 слайсов, курица 30 г, говядина кубиком 44 г, бекон 20 г', 'Курица 60 г, бекон 35 г, красный лук 15 г', 'Говядина 44 г, огурцы 30 г, томаты 15 г', 'Форель 50 г, руккола 20 г, черри 60 г'], correct: 0, note: 'Пицца «Мясная»: ветчина 16 слайсов, курица 30 г, говядина кубиком 44 г, бекон 20 г.' },
];

const shawarmaQuestions: Question[] = [
  { id: 1, text: 'Визуальная проверка: в карточке шаурмы один параметр не соответствует стандарту. Что нужно исправить?', options: ['СВЧ: изменить 0:30, кнопку 3', 'Направление сборки: собирать строго снизу вверх', 'Пергамент: использовать два листа', 'Овощи: положить до разогрева в СВЧ'], correct: 1, note: 'Шаурму собирают строго снизу вверх, отдельными слоями. Первые три слоя разогревают 0:30 на кнопке 3, затем добавляют овощи; используется один лист пергамента.', image: '/training-assets/shawarma-box.png', imageAlt: 'Упаковка шаурмы', visualCheck: { label: 'НАЙДИТЕ ОШИБКУ', title: 'Шаурма · чек сборки', fields: [{ label: 'Направление сборки', value: 'Сверху вниз' }, { label: 'СВЧ первых 3 слоёв', value: '00:30 · кнопка 3' }, { label: 'Пергамент', value: '1 лист' }], helper: 'Сверьте порядок сборки, режим СВЧ и пергамент.' } },
  { id: 2, text: 'На скольких листах пергамента нужно собирать и жарить шаурму?', options: ['На одном листе', 'На двух листах', 'Только на фольге', 'Без пергамента'], correct: 0, note: 'Тортилья, сборка и гриль выполняются на одном листе пергамента.' },
  { id: 3, text: 'Какие слои разогревают в СВЧ до добавления овощей?', options: ['Тортилью, нижний соус и куриное филе', 'Тортилью, айсберг и томаты', 'Огурцы, томаты и айсберг', 'Всю собранную шаурму'], correct: 0, note: 'В СВЧ разогревают первые три слоя: тортилью, нижний соус и куриное филе.' },
  { id: 4, text: 'Какой режим СВЧ используют для первых трёх слоёв?', options: ['0:30, кнопка 3', '1:00, кнопка 5', '1:30, кнопка 6', '2:00, кнопка 7'], correct: 0, note: 'Первые три слоя разогревают в СВЧ 0:30, кнопка 3.' },
  { id: 5, text: 'Сколько куриного филе в любой шаурме по актуальному стандарту?', options: ['30 г', '40 г', '50 г', '60 г'], correct: 1, note: 'Во всех трёх вариантах используется 40 г куриного филе.' },
  { id: 6, text: 'Сколько полосок нижнего соуса в классической шаурме?', options: ['2 полоски', '3 полоски', '4 полоски', '5 полосок'], correct: 2, note: 'Нижний слой соуса для шаурмы — 4 полоски.' },
  { id: 7, text: 'Сколько айсберга добавляют в шаурму?', options: ['16 г', '20 г', '30 г', '40 г'], correct: 2, note: 'Айсберг — 30 г.' },
  { id: 8, text: 'Сколько томатов добавляют в шаурму?', options: ['10 г', '16 г', '20 г', '30 г'], correct: 1, note: 'Томаты — 16 г.' },
  { id: 9, text: 'Сколько маринованных огурцов добавляют в шаурму?', options: ['10 г', '12 г', '16 г', '20 г'], correct: 2, note: 'Маринованные огурцы — 16 г.' },
  { id: 10, text: 'Какой верхний соус у классической шаурмы?', options: ['Соус для шаурмы, 3 полоски', 'Сырный соус, 3 полоски', 'Соус для шаурмы, 4 полоски', 'Шрирача, 1 полоска'], correct: 0, note: 'В классической шаурме верхний соус — соус для шаурмы, 3 полоски.' },
  { id: 11, text: 'Что добавляют к шаурме после разогрева первых трёх слоёв в СВЧ?', options: ['Только верхний соус', 'Только халапеньо', 'Айсберг, томаты и маринованные огурцы', 'Сразу коробку и этикетку'], correct: 2, note: 'После разогрева тортильи, нижнего соуса и филе добавляют овощи: айсберг, томаты и маринованные огурцы.' },
  { id: 12, text: 'Сколько шаурма готовится в прижимном гриле?', options: ['1:00', '1:30', '2:00', '3:00'], correct: 2, note: 'После заворачивания шаурму готовят в прижимном гриле 2:00.' },
  { id: 13, text: 'На чём готовят шаурму в прижимном гриле?', options: ['На том же листе пергамента', 'На новом листе пергамента', 'На фольге', 'Без пергамента'], correct: 0, note: 'Шаурму готовят в гриле на том же листе пергамента.' },
  { id: 14, text: 'Где нужно разместить начинку и соус перед заворачиванием?', options: ['В нижней трети тортильи', 'Строго по центру', 'В верхней трети', 'По всей поверхности'], correct: 0, note: 'Начинку и соус размещают в нижней трети тортильи.' },
  { id: 15, text: 'Какова правильная последовательность заворачивания шаурмы?', options: ['Бока внутрь → нижний край вверх → свернуть в рулет', 'Сначала верхний край → затем бока', 'Сразу свернуть в рулет', 'Нижний край вниз → бока наружу'], correct: 0, note: 'Сначала заверните бока внутрь, затем нижний край вверх, после чего сверните в рулет.' },
  { id: 16, text: 'Какой верхний соус у сырной шаурмы?', options: ['Сырный соус, 3 полоски', 'Сырный соус, 4 полоски', 'Соус для шаурмы, 3 полоски', 'Шрирача, 1 полоска'], correct: 0, note: 'У сырной шаурмы верхний слой — сырный соус, 3 полоски.' },
  { id: 17, text: 'Какой нижний соус у сырной шаурмы?', options: ['Сырный соус, 4 полоски', 'Сырный соус, 3 полоски', 'Соус для шаурмы, 4 полоски', 'Соус для шаурмы и шрирача, 2 + 1 полоска'], correct: 0, note: 'В СВЧ-группе сырной шаурмы наносится сырный соус — 4 полоски.' },
  { id: 18, text: 'Что отличает острую шаурму от классической после овощей?', options: ['Соус для шаурмы + шрирача 2 + 1 полоска и халапеньо 10 г', 'Сырный соус 3 полоски и бекон 35 г', 'Тартар 15 г и рыбные палочки', 'Брусничный соус 20 г и сырная котлета'], correct: 0, note: 'В острой шаурме после овощей: соус для шаурмы + шрирача 2 + 1 полоска, затем халапеньо 10 г.' },
  { id: 19, text: 'Сколько халапеньо добавляют в острую шаурму?', options: ['5 г', '8 г', '10 г', '16 г'], correct: 2, note: 'Халапеньо в острой шаурме — 10 г.' },
  { id: 20, text: 'Сколько полосок шрирачи в острой шаурме?', options: ['1 полоска', '2 полоски', '3 полоски', '4 полоски'], correct: 0, note: 'В острой шаурме добавляют 1 полоску шрирачи.' },
  { id: 21, text: 'Какой нижний соус используют в острой шаурме перед СВЧ?', options: ['Соус для шаурмы, 4 полоски', 'Сырный соус, 4 полоски', 'Соус для шаурмы + шрирача, 2 + 1 полоска', 'Сырный соус, 3 полоски'], correct: 0, note: 'Нижний соус в острой шаурме — соус для шаурмы, 4 полоски.' },
  { id: 22, text: 'Что из перечисленного одинаково для классической, сырной и острой шаурмы?', options: ['Тортилья 1 шт, куриное филе 40 г, айсберг 30 г, томаты и огурцы по 16 г', 'Халапеньо 10 г и шрирача', 'Сырный соус 3 и 4 полоски', 'Верхний соус для шаурмы 3 полоски'], correct: 0, note: 'База одинакова: тортилья 1 шт, филе 40 г, айсберг 30 г, томаты и огурцы по 16 г.' },
  { id: 23, text: 'Что добавляют после овощей в острую шаурму?', options: ['Сырный соус — 3 полоски', 'Соус для шаурмы и шрирача — 2 + 1 полоска, затем халапеньо 10 г', 'Только соус для шаурмы — 3 полоски', 'Ничего — сразу заворачивают'], correct: 1, note: 'В острой шаурме после овощей: соус для шаурмы и шрирача — 2 + 1 полоска, затем халапеньо 10 г.' },
  { id: 24, text: 'Какой порядок упаковки после гриля?', options: ['Коробка → крафт-пакет → этикетка', 'Крафт-пакет → коробка → этикетка', 'Сразу крафт-пакет', 'Коробка → этикетка → крафт-пакет'], correct: 0, note: 'После гриля шаурму кладут в коробку, затем в крафт-пакет и наклеивают этикетку.' },
  { id: 25, text: 'Что нельзя менять при переходе между тремя видами шаурмы?', options: ['Время гриля 2:00 и один лист пергамента', 'Верхний соус', 'Наличие халапеньо', 'Тип соуса в СВЧ-группе'], correct: 0, note: 'Для всех трёх видов неизменны: гриль 2:00 и один лист пергамента.' },
  { id: 26, text: 'Когда добавляют овощи относительно разогрева в СВЧ?', options: ['После разогрева первых трёх слоёв', 'До разогрева вместе с тортильей', 'После прижимного гриля', 'Овощи не добавляют'], correct: 0, note: 'Овощи добавляют после разогрева в СВЧ первых трёх слоёв.' },
];

const categories: Category[] = [
  { id: 'fryer', title: 'Фритюр', subtitle: '26 вопросов · температура, время, порции и порядок', icon: '♨', color: 'orange', questions: fryerQuestions, passScore: 21 },
  { id: 'burgers', title: 'Бургеры', subtitle: '26 вопросов · сборка, веса ингредиентов и отдача', icon: '▤', color: 'red', questions: burgerQuestions, passScore: 21 },
  { id: 'pizza', title: 'Пицца', subtitle: '26 вопросов · слои, веса и работа с печью', icon: '◒', color: 'yellow', questions: pizzaQuestions, passScore: 21 },
  { id: 'shawarma', title: 'Шаурма', subtitle: '26 вопросов · сборка, граммовки и упаковка', icon: '≋', color: 'green', questions: shawarmaQuestions, passScore: 21 },
  { id: 'coffee', title: 'Кофе', subtitle: '16 вопросов · горячие и холодные напитки, сиропы и подача', icon: '◉', color: 'coffee', questions: coffeeQuestions, passScore: 13 },
  { id: 'lemonades', title: 'Лимонады', subtitle: '16 вопросов · основы, лёд, вода и подача', icon: '✦', color: 'blue', questions: lemonadeQuestions, passScore: 13 },
  { id: 'asian', title: 'Азиатская линейка', subtitle: '26 вопросов · бургеры, фритюр, рамен и крылья', icon: '◈', color: 'purple', questions: asianQuestions, passScore: 21 },
  { id: 'russian', title: 'Русская кухня', subtitle: '16 вопросов · бургер, драники и лимонад', icon: '✳', color: 'rose', questions: russianQuestions, passScore: 13 },
  { id: 'expiry', title: 'Сроки годности', subtitle: 'Калькулятор и 10 вопросов · вскрытие, дефростация и маркировка', icon: '◷', color: 'blue', questions: expiryQuestions, passScore: 8 },
];

const passScore = 21;

export default function Home() {
  const [screen, setScreen] = useState<'welcome' | 'categories' | 'levels' | 'expiryCalculator' | 'quiz' | 'result' | 'dashboard' | 'myResults' | 'managerLogin'>('welcome');
  const [employee, setEmployee] = useState('');
  const [activeCategory, setActiveCategory] = useState<Category | null>(null);
  const [trainingLevel, setTrainingLevel] = useState<TrainingLevel>('basic');
  const [questionIndex, setQuestionIndex] = useState(0);
  const [answers, setAnswers] = useState<number[]>([]);
  const [selected, setSelected] = useState<number | null>(null);
  const [attempts, setAttempts] = useState<Attempt[]>([]);
  const [saveState, setSaveState] = useState<'idle' | 'saving' | 'saved' | 'error'>('idle');
  const [journalLoading, setJournalLoading] = useState(false);
  const [managerAuthenticated, setManagerAuthenticated] = useState(false);
  const [managerPassword, setManagerPassword] = useState('');
  const [managerLoginState, setManagerLoginState] = useState<'idle' | 'loading' | 'error'>('idle');
  const [teamMembers, setTeamMembers] = useState<TeamMember[]>([]);
  const [managerTab, setManagerTab] = useState<ManagerTab>('summary');
  const [selectedEmployee, setSelectedEmployee] = useState<string | null>(null);
  const [employeeFilter, setEmployeeFilter] = useState('');
  const [categoryFilter, setCategoryFilter] = useState('all');
  const [statusFilter, setStatusFilter] = useState<'all' | ProgressStatus>('all');
  const [dateFilter, setDateFilter] = useState('');
  const [teamDraft, setTeamDraft] = useState('');
  const [teamActionState, setTeamActionState] = useState<'idle' | 'saving' | 'error'>('idle');
  const [staffMembers, setStaffMembers] = useState<StaffMember[]>([]);
  const [selectedStaffId, setSelectedStaffId] = useState('');
  const [staffPin, setStaffPin] = useState('');
  const [staffAuthenticated, setStaffAuthenticated] = useState(false);
  const [staffLoginState, setStaffLoginState] = useState<'idle' | 'loading' | 'error'>('idle');
  const [issuedPins, setIssuedPins] = useState<IssuedPin[]>([]);
  const [deadlines, setDeadlines] = useState<Record<string, string>>({});
  const [deadlineDrafts, setDeadlineDrafts] = useState<Record<string, string>>({});
  const [deadlineState, setDeadlineState] = useState<'idle' | 'saving' | 'error'>('idle');
  const [expiryItemId, setExpiryItemId] = useState(expiryItems[0]?.id ?? '');
  const [expiryStartedAt, setExpiryStartedAt] = useState(() => {
    const now = new Date();
    const pad = (value: number) => String(value).padStart(2, '0');
    return `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())}T${pad(now.getHours())}:${pad(now.getMinutes())}`;
  });
  const today = useMemo(() => new Intl.DateTimeFormat('ru-RU', { day: 'numeric', month: 'long', year: 'numeric' }).format(new Date()), []);
  const questions = activeCategory ? (trainingLevel === 'advanced' ? advancedQuestions[activeCategory.id] ?? [] : activeCategory.questions ?? []) : [];
  const activeAttemptCategoryId = activeCategory ? (trainingLevel === 'advanced' ? `${activeCategory.id}-level-2` : activeCategory.id) : '';
  const levelTitle = trainingLevel === 'advanced' ? 'Уровень 2' : 'Базовый уровень';
  const score = answers.reduce((sum, answer, index) => sum + Number(answer === questions[index]?.correct), 0);
  const currentPassScore = trainingLevel === 'advanced' ? 5 : activeCategory?.passScore ?? passScore;
  const passed = score >= currentPassScore;
  const expiryItem = expiryItems.find((item) => item.id === expiryItemId) ?? expiryItems[0];
  const expiryLabelMode: ExpiryLabelMode = expiryItem?.labelMode ?? (expiryItem?.openedHours ? 'opening-and-expiry' : 'defrost-and-expiry');
  const expiryHours = expiryLabelMode === 'defrost-and-expiry' ? expiryItem?.defrostHours : expiryLabelMode === 'opening-and-expiry' ? expiryItem?.openedHours : undefined;
  const expiryRule = expiryLabelMode === 'defrost-and-expiry' ? expiryItem?.defrostRule : expiryLabelMode === 'opening-and-expiry' ? expiryItem?.openedRule : undefined;
  const expiryDeadline = expiryHours && expiryStartedAt ? new Date(new Date(expiryStartedAt).getTime() + expiryHours * 60 * 60 * 1000) : null;
  const expiryStartLabel = expiryLabelMode === 'defrost-and-expiry' ? 'Дата и время дефростации' : 'Дата и время открытия';
  const expiryLabelInstruction = expiryLabelMode === 'opening-only' ? 'Напечатайте этикетку только с датой открытия.' : `Напечатайте этикетку с ${expiryLabelMode === 'defrost-and-expiry' ? 'датой дефростации' : 'датой открытия'} и датой/временем списания.`;
  const formatExpiryDate = (date: Date) => new Intl.DateTimeFormat('ru-RU', { day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit' }).format(date);
  useEffect(() => { void fetch('/api/manager/session').then(async (response) => response.ok ? response.json() as Promise<{ authenticated: boolean }> : { authenticated: false }).then((data) => setManagerAuthenticated(Boolean(data.authenticated))).catch(() => setManagerAuthenticated(false)); }, []);
  useEffect(() => { void Promise.all([fetch('/api/staff'), fetch('/api/staff/session')]).then(async ([membersResponse, sessionResponse]) => { const members = membersResponse.ok ? await membersResponse.json() as { members: StaffMember[] } : { members: [] }; const session = sessionResponse.ok ? await sessionResponse.json() as { authenticated: boolean; member: StaffMember | null } : { authenticated: false, member: null }; setStaffMembers(members.members); if (session.authenticated && session.member) { setStaffAuthenticated(true); setEmployee(session.member.name); setSelectedStaffId(String(session.member.id)); } }).catch(() => undefined); }, []);
  const openCategory = (category: Category) => { if (!category.questions) return; setActiveCategory(category); setScreen('levels'); };
  const startLevel = (level: TrainingLevel) => { setTrainingLevel(level); setQuestionIndex(0); setAnswers([]); setSelected(null); setSaveState('idle'); setScreen('quiz'); };
  const recordAttempt = async (finalAnswers: number[]) => {
    if (!activeCategory) return;
    const finalScore = finalAnswers.reduce((sum, answer, index) => sum + Number(answer === questions[index]?.correct), 0);
    setSaveState('saving');
    try {
      const response = await fetch('/api/attempts', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ categoryId: activeAttemptCategoryId, categoryTitle: `${activeCategory.title} · ${levelTitle}`, answers: finalAnswers }) });
      if (!response.ok) throw new Error('save failed');
      const data = await response.json() as { attempt: Attempt };
      setAttempts((current) => [data.attempt, ...current]);
      setSaveState('saved');
    } catch { setSaveState('error'); }
  };
  const loadJournal = async (authorized = managerAuthenticated) => {
    if (!authorized) { setScreen('managerLogin'); return; }
    setJournalLoading(true);
    try {
      const [attemptResponse, teamResponse, deadlineResponse] = await Promise.all([fetch('/api/attempts'), fetch('/api/team'), fetch('/api/deadlines')]);
      if (attemptResponse.status === 401 || teamResponse.status === 401 || deadlineResponse.status === 401) { setManagerAuthenticated(false); setScreen('managerLogin'); return; }
      if (!attemptResponse.ok || !teamResponse.ok || !deadlineResponse.ok) throw new Error('load failed');
      const [attemptData, teamData, deadlineData] = await Promise.all([attemptResponse.json() as Promise<{ attempts: Attempt[] }>, teamResponse.json() as Promise<{ members: TeamMember[] }>, deadlineResponse.json() as Promise<{ deadlines: Record<string, string> }>]);
      setAttempts(attemptData.attempts);
      setTeamMembers(teamData.members);
      setDeadlines(deadlineData.deadlines); setDeadlineDrafts(deadlineData.deadlines);
    } finally { setJournalLoading(false); setScreen('dashboard'); }
  };
  const loadMyResults = async () => {
    if (!employee.trim()) return;
    setJournalLoading(true);
    try {
      const response = await fetch('/api/attempts');
      if (!response.ok) throw new Error('load failed');
      const data = await response.json() as { attempts: Attempt[] };
      setAttempts(data.attempts);
      setScreen('myResults');
    } finally { setJournalLoading(false); }
  };
  const signInManager = async (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    if (!managerPassword) return;
    setManagerLoginState('loading');
    try {
      const response = await fetch('/api/manager/login', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ password: managerPassword }) });
      if (!response.ok) throw new Error('login failed');
      setManagerAuthenticated(true);
      setManagerPassword('');
      setManagerLoginState('idle');
      void loadJournal(true);
    } catch { setManagerLoginState('error'); }
  };
  const signOutManager = async () => {
    await fetch('/api/manager/logout', { method: 'POST' }).catch(() => undefined);
    setManagerAuthenticated(false);
    setAttempts([]);
    setTeamMembers([]);
    setSelectedEmployee(null);
    setScreen(employee.trim() ? 'categories' : 'welcome');
  };
  const signInStaff = async (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const memberId = Number(selectedStaffId);
    if (!memberId || !staffPin) return;
    setStaffLoginState('loading');
    try {
      const response = await fetch('/api/staff/login', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ memberId, pin: staffPin }) });
      if (!response.ok) throw new Error('staff login failed');
      const data = await response.json() as { member: StaffMember };
      setEmployee(data.member.name); setSelectedStaffId(String(data.member.id)); setStaffPin(''); setStaffAuthenticated(true); setStaffLoginState('idle'); setScreen('categories');
    } catch { setStaffLoginState('error'); }
  };
  const signOutStaff = async () => {
    await fetch('/api/staff/logout', { method: 'POST' }).catch(() => undefined);
    setStaffAuthenticated(false); setEmployee(''); setSelectedStaffId(''); setStaffPin(''); setAttempts([]); setScreen('welcome');
  };
  const saveTeamMembers = async (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const names = teamDraft.split(/[\n,;]+/).map((name) => name.trim()).filter(Boolean);
    if (!names.length) return;
    setTeamActionState('saving');
    try {
      const response = await fetch('/api/team', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ names }) });
      if (!response.ok) throw new Error('team save failed');
      const data = await response.json() as { members: TeamMember[]; issuedPins: IssuedPin[] };
      setTeamMembers(data.members);
      setStaffMembers(data.members.map(({ id, name }) => ({ id, name })));
      setIssuedPins(data.issuedPins);
      setTeamDraft('');
      setTeamActionState('idle');
    } catch { setTeamActionState('error'); }
  };
  const removeTeamMember = async (member: TeamMember) => {
    if (!window.confirm(`Убрать ${member.name} из активного списка? История попыток сохранится.`)) return;
    const response = await fetch(`/api/team?id=${member.id}`, { method: 'DELETE' });
    if (!response.ok) return;
    setTeamMembers((current) => current.filter((item) => item.id !== member.id));
    setStaffMembers((current) => current.filter((item) => item.id !== member.id));
    if (selectedEmployee === member.name) setSelectedEmployee(null);
  };
  const reissuePin = async (member: TeamMember) => {
    const response = await fetch('/api/team/pin', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ memberId: member.id }) });
    if (!response.ok) return;
    const data = await response.json() as { issuedPin: IssuedPin };
    setIssuedPins((current) => [data.issuedPin, ...current.filter((item) => item.id !== member.id)]);
    setTeamMembers((current) => current.map((item) => item.id === member.id ? { ...item, pinConfigured: true } : item));
  };
  const exportPins = () => {
    const rows = [['Сотрудник', 'Личный PIN'], ...issuedPins.map((item) => [item.name, item.pin])];
    const file = new Blob([`\ufeff${rows.map((row) => row.map((value) => `"${value.replaceAll('"', '""')}"`).join(';')).join('\n')}`], { type: 'text/csv;charset=utf-8' });
    const url = URL.createObjectURL(file); const link = document.createElement('a');
    link.href = url; link.download = `личные-pin-${new Date().toISOString().slice(0, 10)}.csv`; link.click(); URL.revokeObjectURL(url);
  };
  const saveDeadlines = async (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault(); setDeadlineState('saving');
    try {
      const response = await fetch('/api/deadlines', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ deadlines: categories.map((category) => ({ categoryId: category.id, dueDate: deadlineDrafts[category.id] ?? '' })) }) });
      if (!response.ok) throw new Error('deadline save failed');
      const data = await response.json() as { deadlines: Record<string, string> };
      setDeadlines(data.deadlines); setDeadlineDrafts(data.deadlines); setDeadlineState('idle');
    } catch { setDeadlineState('error'); }
  };
  const nextQuestion = () => { if (selected === null) return; const nextAnswers = [...answers, selected]; if (questionIndex + 1 === questions.length) { setAnswers(nextAnswers); setScreen('result'); void recordAttempt(nextAnswers); return; } setAnswers(nextAnswers); setQuestionIndex((index) => index + 1); setSelected(null); };
  const retry = () => { setQuestionIndex(0); setAnswers([]); setSelected(null); setScreen('quiz'); };
  const passRate = attempts.length ? Math.round((attempts.filter((attempt) => attempt.passed).length / attempts.length) * 100) : 0;
  const averageScore = attempts.length ? Math.round((attempts.reduce((sum, attempt) => sum + (attempt.score / attempt.total) * 100, 0) / attempts.length)) : 0;
  const normalizeName = (name: string) => name.trim().replace(/\s+/g, ' ').toLocaleLowerCase('ru-RU');
  const attemptKey = (employeeName: string, categoryId: string) => `${normalizeName(employeeName)}::${categoryId}`;
  const latestByEmployeeCategory = useMemo(() => {
    const latest = new Map<string, Attempt>();
    for (const attempt of attempts) { const key = attemptKey(attempt.employeeName, attempt.categoryId); if (!latest.has(key)) latest.set(key, attempt); }
    return latest;
  }, [attempts]);
  const currentStatus = (memberName: string, categoryId: string): ProgressStatus => {
    const attempt = latestByEmployeeCategory.get(attemptKey(memberName, categoryId));
    return !attempt ? 'missing' : attempt.passed ? 'passed' : 'failed';
  };
  const shownCategories = categoryFilter === 'all' ? categories : categories.filter((category) => category.id === categoryFilter);
  const filteredTeam = teamMembers.filter((member) => {
    if (employeeFilter && !normalizeName(member.name).includes(normalizeName(employeeFilter))) return false;
    return statusFilter === 'all' || shownCategories.some((category) => currentStatus(member.name, category.id) === statusFilter);
  });
  const baseCategoryId = (categoryId: string) => categoryId.endsWith('-level-2') ? categoryId.slice(0, -8) : categoryId;
  const filteredAttempts = attempts.filter((attempt) => {
    if (employeeFilter && !normalizeName(attempt.employeeName).includes(normalizeName(employeeFilter))) return false;
    if (categoryFilter !== 'all' && baseCategoryId(attempt.categoryId) !== categoryFilter) return false;
    if (statusFilter === 'passed' && !attempt.passed) return false;
    if (statusFilter === 'failed' && attempt.passed) return false;
    if (statusFilter === 'missing') return false;
    return !dateFilter || attempt.completedAt.slice(0, 10) === dateFilter;
  });
  const allTeamStatuses = teamMembers.flatMap((member) => categories.map((category) => currentStatus(member.name, category.id)));
  const passedCategories = allTeamStatuses.filter((status) => status === 'passed').length;
  const employeesToRepeat = teamMembers.filter((member) => categories.some((category) => currentStatus(member.name, category.id) === 'failed')).length;
  const employeesNotStarted = teamMembers.filter((member) => categories.every((category) => currentStatus(member.name, category.id) === 'missing')).length;
  const todayIso = new Date().toISOString().slice(0, 10);
  const isOverdue = (memberName: string, categoryId: string) => Boolean(deadlines[categoryId] && deadlines[categoryId] < todayIso && currentStatus(memberName, categoryId) !== 'passed');
  const overdueCategories = teamMembers.flatMap((member) => categories.filter((category) => isOverdue(member.name, category.id))).length;
  const selectedEmployeeAttempts = selectedEmployee ? attempts.filter((attempt) => normalizeName(attempt.employeeName) === normalizeName(selectedEmployee)) : [];
  const selectedEmployeeBest = selectedEmployeeAttempts.reduce<Attempt | null>((best, attempt) => !best || attempt.score / attempt.total > best.score / best.total ? attempt : best, null);
  const exportAttempts = () => {
    const quote = (value: string | number) => `"${String(value).replaceAll('"', '""')}"`;
    const rows = [['Сотрудник', 'Категория', 'Дата и время', 'Результат', 'Статус'], ...filteredAttempts.map((attempt) => [attempt.employeeName, attempt.categoryTitle, new Date(attempt.completedAt).toLocaleString('ru-RU'), `${attempt.score} / ${attempt.total}`, attempt.passed ? 'Зачёт' : 'Повторить'])];
    const file = new Blob([`\ufeff${rows.map((row) => row.map(quote).join(';')).join('\n')}`], { type: 'text/csv;charset=utf-8' });
    const url = URL.createObjectURL(file);
    const link = document.createElement('a');
    link.href = url; link.download = `результаты-обучения-${new Date().toISOString().slice(0, 10)}.csv`; link.click(); URL.revokeObjectURL(url);
  };
  const topicForQuestion = (question: Question) => {
    const text = question.text.toLocaleLowerCase('ru-RU');
    if (question.visualCheck) return 'Визуальная проверка';
    if (text.includes('температур') || text.includes('°c')) return 'Температура';
    if (text.includes('сколько готов') || text.includes('время') || text.includes('свч') || text.includes('грил') || text.includes('печ')) return 'Время приготовления';
    if (text.includes('сколько ') || text.includes('какая масса') || text.includes('какой выход') || text.includes('фасовк') || text.includes('порци')) return 'Граммовка и порция';
    if (text.includes('упаков') || text.includes('отдают') || text.includes('пакет') || text.includes('крышк')) return 'Упаковка и выдача';
    if (text.includes('поряд') || text.includes('последователь') || text.includes('слой') || text.includes('направлен')) return 'Порядок приготовления';
    return 'Рецептура и ингредиенты';
  };
  const errorTopics = useMemo(() => {
    const summary = new Map<string, number>();
    for (const attempt of attempts) {
      const source = attempt.categoryId.endsWith('-level-2') ? advancedQuestions[baseCategoryId(attempt.categoryId)] : categories.find((category) => category.id === attempt.categoryId)?.questions;
      if (!source || !attempt.answers) continue;
      source.forEach((question, index) => { if (attempt.answers?.[index] !== question.correct) { const topic = topicForQuestion(question); summary.set(topic, (summary.get(topic) ?? 0) + 1); } });
    }
    return [...summary.entries()].sort((left, right) => right[1] - left[1]).slice(0, 5);
  }, [attempts]);

  if (screen === 'quiz' && activeCategory && questions[questionIndex]) { const question = questions[questionIndex]; return <main className="trainer-shell"><header className="topbar"><button className="brand" onClick={() => setScreen('categories')} aria-label="К выбору категории"><span className="brand-mark">S</span><span><b>STANDART</b><small>Тренажёр знаний</small></span></button><div className="topbar-meta"><span className="employee-chip">{employee}</span><span className="date-chip">{today}</span></div></header><section className="quiz-view"><div className="quiz-topline"><button className="back-button" onClick={() => setScreen('levels')}>← К уровням</button><span>{activeCategory.title} · {levelTitle} · вопрос {questionIndex + 1} из {questions.length}</span></div><div className="progress-track" aria-label={`Выполнено ${questionIndex + 1} из ${questions.length}`}><i style={{ width: `${((questionIndex + 1) / questions.length) * 100}%` }} /></div><article className="question-card"><span className="question-number">{String(questionIndex + 1).padStart(2, '0')}</span><p className="question-kicker">{question.visualCheck ? 'ВИЗУАЛЬНАЯ ПРОВЕРКА · ВЫБЕРИТЕ ОДИН ВАРИАНТ' : 'ВЫБЕРИТЕ ОДИН ВАРИАНТ'}</p>{question.image && <figure className="question-visual"><img src={question.image} alt={question.imageAlt ?? 'Визуальный материал стандарта'} /><figcaption>{question.visualCheck ? 'Фото или схема с рабочего поста' : 'Визуальная карточка стандарта'}</figcaption></figure>}{question.visualCheck && <aside className="visual-check-card" aria-label={question.visualCheck.title}><div className="visual-check-heading"><span>{question.visualCheck.label}</span><b>{question.visualCheck.title}</b></div><div className="visual-check-fields">{question.visualCheck.fields.map((field) => <div key={field.label}><small>{field.label}</small><strong>{field.value}</strong></div>)}</div>{question.visualCheck.helper && <p>{question.visualCheck.helper}</p>}</aside>}<h1>{question.text}</h1><div className="answer-list">{question.options.map((option, index) => <button key={option} className={`answer-option ${selected === index ? 'selected' : ''}`} onClick={() => setSelected(index)} aria-pressed={selected === index}><span>{String.fromCharCode(65 + index)}</span><p>{option}</p><i aria-hidden="true" /></button>)}</div><div className="question-actions"><span>{selected === null ? 'Выберите вариант ответа' : 'Ответ выбран'}</span><button className="primary-button" onClick={nextQuestion} disabled={selected === null}>{questionIndex + 1 === questions.length ? 'Завершить тест' : 'Следующий вопрос'} <b>→</b></button></div></article></section></main>; }

  if (screen === 'dashboard') return <main className="trainer-shell"><header className="topbar"><button className="brand" onClick={() => setScreen('categories')} aria-label="К выбору категории"><span className="brand-mark">S</span><span><b>STANDART</b><small>Тренажёр знаний</small></span></button><div className="topbar-meta"><button className="journal-button" onClick={() => void loadJournal()} disabled={journalLoading}>{journalLoading ? 'Обновляем…' : 'Обновить данные'}</button><button className="staff-switch-button" onClick={() => void signOutManager()}>Выйти</button><span className="date-chip">{today}</span></div></header><section className="dashboard-view manager-dashboard"><div className="dashboard-top"><div><button className="back-button" onClick={() => setScreen('categories')}>← К категориям</button><p className="eyebrow">ПАНЕЛЬ РУКОВОДИТЕЛЯ</p><h1>Контроль обучения</h1></div></div><div className="manager-stat-grid"><article><span>В команде</span><b>{teamMembers.length}</b><small>сотрудников в реестре</small></article><article><span>Категории сданы</span><b>{passedCategories} <em>/ {teamMembers.length * categories.length}</em></b><small>по последней попытке</small></article><article><span>Нужна пересдача</span><b>{employeesToRepeat}</b><small>сотрудников с ошибкой</small></article><article><span>Ещё не сдавали</span><b>{employeesNotStarted}</b><small>ни одной категории</small></article><article><span>Просрочено</span><b>{overdueCategories}</b><small>категорий после срока</small></article><article><span>Средний балл</span><b>{averageScore}%</b><small>по всем попыткам</small></article></div><div className="manager-tabs" role="tablist"><button className={managerTab === 'summary' ? 'active' : ''} onClick={() => setManagerTab('summary')}>Сводка команды</button><button className={managerTab === 'attempts' ? 'active' : ''} onClick={() => setManagerTab('attempts')}>Журнал попыток</button><button className={managerTab === 'team' ? 'active' : ''} onClick={() => setManagerTab('team')}>Сотрудники</button><button className={managerTab === 'deadlines' ? 'active' : ''} onClick={() => setManagerTab('deadlines')}>Сроки</button></div>{managerTab !== 'team' && managerTab !== 'deadlines' && <div className="manager-filters"><label><span>Сотрудник</span><input value={employeeFilter} onChange={(event) => setEmployeeFilter(event.target.value)} placeholder="Поиск по ФИО" /></label><label><span>Категория</span><select value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)}><option value="all">Все категории</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.title}</option>)}</select></label><label><span>Статус</span><select value={statusFilter} onChange={(event) => setStatusFilter(event.target.value as 'all' | ProgressStatus)}><option value="all">Все статусы</option><option value="passed">Сдано</option><option value="failed">Нужна пересдача</option>{managerTab === 'summary' && <option value="missing">Не сдавали</option>}</select></label>{managerTab === 'attempts' && <label><span>Дата попытки</span><input type="date" value={dateFilter} onChange={(event) => setDateFilter(event.target.value)} /></label>}</div>}{managerTab === 'summary' && <><div className="manager-section-heading"><div><h2>Матрица прохождения</h2><p>Нажмите на сотрудника или результат, чтобы открыть его карточку.</p></div><span>{filteredTeam.length} из {teamMembers.length}</span></div>{teamMembers.length === 0 ? <div className="manager-empty"><b>Сначала добавьте команду</b><p>Перейдите во вкладку «Сотрудники», вставьте список ФИО и сразу скачайте автоматически созданные PIN-коды.</p><button className="secondary-button" onClick={() => setManagerTab('team')}>Добавить сотрудников</button></div> : <div className="matrix-table"><div className="matrix-row matrix-head" style={{ gridTemplateColumns: `minmax(190px, 1.5fr) repeat(${shownCategories.length}, minmax(112px, 1fr))` }}><span>Сотрудник</span>{shownCategories.map((category) => <span key={category.id}>{category.title}</span>)}</div>{filteredTeam.length === 0 ? <p className="empty-attempts">По текущему фильтру сотрудников не найдено.</p> : filteredTeam.map((member) => <div className="matrix-row" key={member.id} style={{ gridTemplateColumns: `minmax(190px, 1.5fr) repeat(${shownCategories.length}, minmax(112px, 1fr))` }}><button className="member-name" onClick={() => setSelectedEmployee(member.name)}>{member.name}</button>{shownCategories.map((category) => { const attempt = latestByEmployeeCategory.get(attemptKey(member.name, category.id)); const status = currentStatus(member.name, category.id); const overdue = isOverdue(member.name, category.id); return <button className={`matrix-cell ${status} ${overdue ? 'overdue' : ''}`} key={category.id} onClick={() => setSelectedEmployee(member.name)}><b>{attempt ? `${attempt.score} / ${attempt.total}` : '—'}</b><small>{overdue ? 'Просрочено' : status === 'passed' ? 'Зачёт' : status === 'failed' ? 'Повторить' : 'Не сдавал'}</small></button>; })}</div>)}</div>}<div className="analytics-card"><div><p className="eyebrow">АНАЛИТИКА ОШИБОК</p><h2>Что повторить команде</h2><p>Показывает темы ошибок по новым попыткам — без просмотра ответов каждого сотрудника вручную.</p></div><div className="topic-list">{errorTopics.length ? errorTopics.map(([topic, amount]) => <div key={topic}><span>{topic}</span><b>{amount}</b></div>) : <p>Появится после первых защищённых прохождений с PIN.</p>}</div></div>{selectedEmployee && <section className="employee-detail"><div className="employee-detail-top"><div><p className="eyebrow">КАРТОЧКА СОТРУДНИКА</p><h2>{selectedEmployee}</h2></div><button className="close-detail" onClick={() => setSelectedEmployee(null)}>Закрыть ×</button></div><div className="detail-summary"><article><span>Всего попыток</span><b>{selectedEmployeeAttempts.length}</b></article><article><span>Последний результат</span><b>{selectedEmployeeAttempts[0] ? `${selectedEmployeeAttempts[0].score} / ${selectedEmployeeAttempts[0].total}` : '—'}</b></article><article><span>Лучший результат</span><b>{selectedEmployeeBest ? `${selectedEmployeeBest.score} / ${selectedEmployeeBest.total}` : '—'}</b></article></div><div className="detail-status-grid">{categories.map((category) => { const attempt = latestByEmployeeCategory.get(attemptKey(selectedEmployee, category.id)); const status = currentStatus(selectedEmployee, category.id); const overdue = isOverdue(selectedEmployee, category.id); return <button key={category.id} className={`detail-status ${status} ${overdue ? 'overdue' : ''}`} onClick={() => setCategoryFilter(category.id)}><span>{category.title}</span><b>{attempt ? `${attempt.score} / ${attempt.total}` : 'Не сдавал'}</b><small>{overdue ? 'Просрочено' : status === 'passed' ? 'Зачёт' : status === 'failed' ? 'Нужна пересдача' : 'Нет попытки'}</small></button>; })}</div><div className="detail-attempts"><h3>Все попытки</h3>{selectedEmployeeAttempts.length === 0 ? <p>Сотрудник ещё не проходил тесты.</p> : selectedEmployeeAttempts.map((attempt) => <div key={attempt.id}><span>{attempt.categoryTitle}</span><span>{new Intl.DateTimeFormat('ru-RU', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }).format(new Date(attempt.completedAt))}</span><b>{attempt.score} / {attempt.total}</b><i className={attempt.passed ? 'status-pass' : 'status-repeat'}>{attempt.passed ? 'Зачёт' : 'Повторить'}</i></div>)}</div></section>}</>}{managerTab === 'attempts' && <><div className="manager-section-heading"><div><h2>Журнал попыток</h2><p>Показывает каждую сдачу; выгрузка учитывает текущие фильтры.</p></div><button className="secondary-button" onClick={exportAttempts}>Скачать CSV</button></div><div className="attempt-table"><div className="attempt-table-head"><span>Сотрудник</span><span>Категория</span><span>Дата</span><span>Результат</span><span>Статус</span></div>{filteredAttempts.length === 0 ? <p className="empty-attempts">Попыток по этому фильтру пока нет.</p> : filteredAttempts.map((attempt) => <div className="attempt-row" key={attempt.id}><button className="attempt-member" onClick={() => { setSelectedEmployee(attempt.employeeName); setManagerTab('summary'); }}>{attempt.employeeName}</button><span>{attempt.categoryTitle}</span><span>{new Intl.DateTimeFormat('ru-RU', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }).format(new Date(attempt.completedAt))}</span><strong>{attempt.score} / {attempt.total}</strong><i className={attempt.passed ? 'status-pass' : 'status-repeat'}>{attempt.passed ? 'Зачёт' : 'Повторить'}</i></div>)}</div></>}{managerTab === 'team' && <><div className="manager-section-heading"><div><h2>Реестр команды и PIN-коды</h2><p>При добавлении новых сотрудников система создаёт личный шестизначный PIN. Его можно скачать и передать сотруднику.</p></div><span>{teamMembers.length} сотрудников</span></div><form className="team-import" onSubmit={(event) => void saveTeamMembers(event)}><label htmlFor="team-members">Список ФИО</label><textarea id="team-members" value={teamDraft} onChange={(event) => { setTeamDraft(event.target.value); setTeamActionState('idle'); }} placeholder={'Одно ФИО на строку\nАнна Соколова\nИван Петров'} /><div><small>Можно вставить до 100 имён за раз: по одному в строке, через запятую или точку с запятой.</small><button className="primary-button" type="submit" disabled={!teamDraft.trim() || teamActionState === 'saving'}>{teamActionState === 'saving' ? 'Сохраняем…' : 'Добавить и создать PIN'} <span>→</span></button></div>{teamActionState === 'error' && <p className="team-error">Не удалось сохранить список. Попробуйте ещё раз.</p>}</form>{issuedPins.length > 0 && <section className="pin-panel"><div><p className="eyebrow">НОВЫЕ PIN-КОДЫ</p><h3>Сохраните их сейчас</h3><p>Коды показываются только после создания или перевыпуска. Скачать таблицу удобнее всего до закрытия страницы.</p></div><button className="secondary-button" onClick={exportPins}>Скачать PIN в CSV</button><div className="pin-list">{issuedPins.map((item) => <div key={item.id}><span>{item.name}</span><b>{item.pin}</b></div>)}</div></section>}<div className="team-list">{teamMembers.length === 0 ? <p className="empty-attempts">Реестр пока пуст.</p> : teamMembers.map((member) => <div key={member.id}><span><b>{member.name}</b><small>{member.pinConfigured ? 'PIN настроен' : 'PIN не задан'}</small></span><div><button onClick={() => void reissuePin(member)}>Новый PIN</button><button onClick={() => void removeTeamMember(member)} aria-label={`Убрать ${member.name} из списка`}>Убрать</button></div></div>)}</div></>}{managerTab === 'deadlines' && <><div className="manager-section-heading"><div><h2>Сроки прохождения</h2><p>Укажите дату для каждой категории. После срока в сводке подсветятся непройденные и несданные категории.</p></div></div><form className="deadline-form" onSubmit={(event) => void saveDeadlines(event)}>{categories.map((category) => <label key={category.id}><span>{category.title}</span><input type="date" value={deadlineDrafts[category.id] ?? ''} onChange={(event) => setDeadlineDrafts((current) => ({ ...current, [category.id]: event.target.value }))} /><small>{deadlines[category.id] ? `Установлен срок: ${new Intl.DateTimeFormat('ru-RU').format(new Date(`${deadlines[category.id]}T00:00:00`))}` : 'Срок не установлен'}</small></label>)}<button className="primary-button" type="submit" disabled={deadlineState === 'saving'}>{deadlineState === 'saving' ? 'Сохраняем…' : 'Сохранить сроки'} <span>→</span></button>{deadlineState === 'error' && <p className="team-error">Не удалось сохранить сроки. Попробуйте ещё раз.</p>}</form></>}</section></main>;

  return <main className="trainer-shell">
    <header className="topbar">
      <button className="brand" onClick={() => screen !== 'welcome' && setScreen('categories')} aria-label="К выбору категории"><span className="brand-mark">МГ</span><span><b>МНОГО ГОРЯЧЕГО</b><small>Тренажёр стандартов</small></span></button>
      <div className="topbar-meta">{staffAuthenticated && <><button className="journal-button my-results-button" onClick={() => void loadMyResults()} disabled={journalLoading}>{journalLoading ? 'Открываем…' : 'Мои результаты'}</button><button className="staff-switch-button" onClick={() => void signOutStaff()}>Сменить сотрудника</button></>}<button className="journal-button" onClick={() => void loadJournal()} disabled={journalLoading}>{managerAuthenticated ? 'Журнал руководителя' : 'Вход руководителя'}</button>{staffAuthenticated && <span className="employee-chip">{employee}</span>}<span className="date-chip">{today}</span></div>
    </header>

    {screen === 'welcome' && <section className="welcome-view">
      <div className="welcome-copy"><p className="eyebrow">ПРОВЕРКА ЗНАНИЙ · НОВЫЕ СТАНДАРТЫ</p><h1>Знай стандарт.<br /><em>Готовь уверенно.</em></h1><p className="lead">Выберите направление, ответьте на вопросы и сразу разберите ошибки — до следующей смены, а не после неё.</p><div className="feature-row"><span>✓ Результат сразу</span><span>✓ Разбор ошибок</span><span>✓ Статистика попыток</span></div></div>
      <form className="identity-card" onSubmit={(event) => void signInStaff(event)}><span className="step-label">01 / ВХОД СОТРУДНИКА</span><h2>Кто проходит тест?</h2><p>Выберите себя и введите личный PIN — так результат точно попадёт в вашу историю.</p><label htmlFor="staff-member">Сотрудник</label><select id="staff-member" value={selectedStaffId} onChange={(event) => { setSelectedStaffId(event.target.value); setStaffLoginState('idle'); }} disabled={staffAuthenticated || staffMembers.length === 0}><option value="">{staffMembers.length ? 'Выберите себя из списка' : 'Руководитель ещё не добавил сотрудников'}</option>{staffMembers.map((member) => <option key={member.id} value={member.id}>{member.name}</option>)}</select><label htmlFor="staff-pin" className="pin-label">Личный PIN</label><input id="staff-pin" type="password" inputMode="numeric" value={staffPin} onChange={(event) => { setStaffPin(event.target.value.replace(/\D/g, '').slice(0, 6)); setStaffLoginState('idle'); }} placeholder="6 цифр" autoComplete="one-time-code" disabled={staffAuthenticated} /><div className="autodate"><span>Дата прохождения</span><b>{today}</b></div><button className="primary-button" type="submit" disabled={staffAuthenticated || !selectedStaffId || staffPin.length !== 6 || staffLoginState === 'loading'}>{staffLoginState === 'loading' ? 'Проверяем…' : 'Выбрать категорию'} <span>→</span></button>{staffLoginState === 'error' && <small className="login-error">Проверьте выбранное имя и PIN.</small>}<small className="privacy-note">PIN выдаёт руководитель в разделе «Сотрудники».</small></form>
    </section>}

    {screen === 'categories' && <section className="category-view"><div className="section-heading"><div><p className="eyebrow">02 / НАПРАВЛЕНИЕ</p><h1>Что повторяем сегодня?</h1></div><p>Все тесты основаны на утверждённых новых стандартах.</p></div><div className="category-grid">{categories.map((category) => <article className={`category-card ${category.color} ${category.questions ? 'ready' : 'soon'}`} key={category.id}><div className="category-icon" aria-hidden="true">{category.icon}</div><div className="category-info"><span>{category.id === 'expiry' ? 'КАЛЬКУЛЯТОР + ТЕСТ' : category.questions ? '2 УРОВНЯ' : 'СКОРО'}</span><h2>{category.title}</h2><p>{category.subtitle}</p></div><button onClick={() => openCategory(category)} disabled={!category.questions}>{category.questions ? category.id === 'expiry' ? 'Открыть раздел' : 'Выбрать уровень' : 'В подготовке'} <b>→</b></button></article>)}</div><p className="category-footnote">Базовый уровень проверяет основу стандартов. Уровень 2 — 6 ситуационных вопросов, из них 3 визуальные; зачёт от 5/6. В разделе «Сроки годности» доступны отдельные калькулятор и тест.</p></section>}

    {screen === 'levels' && activeCategory && <section className="level-view"><button className="back-button" onClick={() => setScreen('categories')}>← К категориям</button><div className="level-heading"><p className="eyebrow">03 / {activeCategory.id === 'expiry' ? 'СРОКИ ГОДНОСТИ' : 'УРОВЕНЬ ТРЕНИРОВКИ'}</p><h1>{activeCategory.title}</h1><p>{activeCategory.id === 'expiry' ? 'Посчитайте дату списания по подтверждённым данным поставщика и закрепите правила коротким тестом.' : 'Сначала закрепите основу, затем переходите к рабочим ситуациям и визуальному контролю.'}</p></div>{activeCategory.id === 'expiry' ? <><div className="level-grid expiry-actions"><article className="level-card advanced"><span>ИНСТРУМЕНТ</span><h2>Калькулятор срока</h2><p>Выберите продукт и дату/время. Калькулятор сам подскажет, какую этикетку печатать.</p><small>{expiryItems.length} подтверждённых позиций от поставщиков</small><button className="primary-button" onClick={() => setScreen('expiryCalculator')}>Открыть калькулятор <b>→</b></button></article><article className="level-card"><span>БАЗОВЫЙ ТЕСТ</span><h2>Проверка сроков</h2><p>10 вопросов: вскрытие, дефростация, температурные условия и проверка маркировки.</p><small>Проходной результат: {activeCategory.passScore ?? passScore} из {activeCategory.questions?.length ?? 0}</small><button className="secondary-button" onClick={() => startLevel('basic')}>Пройти тест <b>→</b></button></article></div><p className="level-note">В расчёт включены только подтверждённые сроки. Всегда сверяйте результат с заводской маркировкой и общим сроком годности.</p></> : <><div className="level-grid"><article className="level-card"><span>УРОВЕНЬ 1</span><h2>База стандартов</h2><p>{activeCategory.questions?.length ?? 0} вопросов: рецептура, граммовки, оборудование и порядок действий.</p><small>Проходной результат: {activeCategory.passScore ?? passScore} из {activeCategory.questions?.length ?? 0}</small><button className="secondary-button" onClick={() => startLevel('basic')}>Начать базовый тест <b>→</b></button></article><article className="level-card advanced"><span>УРОВЕНЬ 2</span><h2>Рабочие ситуации</h2><p>6 более сложных вопросов: проверка карточек, сочетание нескольких параметров и 3 визуальные ситуации.</p><small>Проходной результат: 5 из 6</small><button className="primary-button" onClick={() => startLevel('advanced')}>Начать уровень 2 <b>→</b></button></article></div><p className="level-note">Результат уровня 2 сохраняется отдельной строкой в личной истории и журнале руководителя.</p></>}</section>}

    {screen === 'expiryCalculator' && <section className="expiry-view"><button className="back-button" onClick={() => setScreen('levels')}>← К разделу «Сроки годности»</button><div className="expiry-heading"><p className="eyebrow">КАЛЬКУЛЯТОР СРОКА ГОДНОСТИ</p><h1>Что печатать на этикетке?</h1><p>Выберите продукт и дату/время. Калькулятор сам определит правило и покажет, какие даты выставить на пистолете.</p></div><div className="expiry-layout"><form className="expiry-form" onSubmit={(event) => event.preventDefault()}><label><span>Продукт</span><select value={expiryItemId} onChange={(event) => setExpiryItemId(event.target.value)}>{expiryItems.map((item) => <option key={item.id} value={item.id}>{item.group} · {item.name}</option>)}</select></label><label><span>{expiryStartLabel}</span><input type="datetime-local" value={expiryStartedAt} onChange={(event) => setExpiryStartedAt(event.target.value)} /></label><aside className="expiry-source"><span>Какую этикетку печатать</span><b>{expiryLabelMode === 'opening-only' ? 'Только дата открытия' : expiryLabelMode === 'defrost-and-expiry' ? 'Дата дефростации + дата списания' : 'Дата открытия + дата списания'}</b><p>{expiryLabelInstruction}</p></aside><aside className="expiry-source"><span>Правило поставщика</span><b>{expiryRule ?? 'Действует общий срок на заводской упаковке'}</b><p>{expiryItem?.condition}</p></aside></form><aside className="expiry-result"><p>{expiryLabelMode === 'opening-only' ? 'ЭТИКЕТКА' : 'СРОК СПИСАНИЯ'}</p>{expiryLabelMode === 'opening-only' ? <><h2>Дата открытия</h2><strong>{formatExpiryDate(new Date(expiryStartedAt))}</strong></> : expiryDeadline ? <><h2>{formatExpiryDate(expiryDeadline)}</h2><strong>{expiryRule}</strong><div className="expiry-label-lines"><span>{expiryStartLabel}</span><b>{formatExpiryDate(new Date(expiryStartedAt))}</b><span>Дата и время списания</span><b>{formatExpiryDate(expiryDeadline)}</b></div></> : <h2>Укажите дату события</h2>}<small>Калькулятор не продлевает общий срок годности и не заменяет заводскую маркировку. При расхождении используйте более раннюю дату.</small></aside></div></section>}

    {screen === 'managerLogin' && <section className="manager-login"><form className="identity-card manager-card" onSubmit={(event) => void signInManager(event)}><button className="back-button" type="button" onClick={() => setScreen(employee.trim() ? 'categories' : 'welcome')}>← Назад</button><p className="eyebrow">ДОСТУП РУКОВОДИТЕЛЯ</p><h1>Журнал команды</h1><p>Введите пароль руководителя. Только после входа доступны результаты всех сотрудников и общая статистика.</p><label htmlFor="manager-password">Пароль руководителя</label><input id="manager-password" type="password" value={managerPassword} onChange={(event) => { setManagerPassword(event.target.value); setManagerLoginState('idle'); }} autoComplete="current-password" placeholder="Введите пароль" /><button className="primary-button" type="submit" disabled={!managerPassword || managerLoginState === 'loading'}>{managerLoginState === 'loading' ? 'Проверяем…' : 'Открыть журнал'} <span>→</span></button>{managerLoginState === 'error' && <small className="login-error">Пароль не подошёл. Проверьте и попробуйте снова.</small>}<small className="privacy-note">Сотрудники через этот сайт видят только собственные попытки.</small></form></section>}

    {screen === 'dashboard' && <section className="dashboard-view manager-dashboard"><div className="dashboard-top"><div><button className="back-button" onClick={() => setScreen('categories')}>← К категориям</button><p className="eyebrow">ПАНЕЛЬ РУКОВОДИТЕЛЯ</p><h1>Контроль обучения</h1></div><div className="dashboard-actions"><button className="secondary-button" onClick={() => void loadJournal()}>{journalLoading ? 'Обновляем…' : 'Обновить'}</button><button className="logout-button" onClick={() => void signOutManager()}>Выйти</button></div></div><div className="manager-stat-grid"><article><span>В команде</span><b>{teamMembers.length}</b><small>сотрудников в реестре</small></article><article><span>Категории сданы</span><b>{passedCategories} <em>/ {teamMembers.length * categories.length}</em></b><small>по последней попытке</small></article><article><span>Нужна пересдача</span><b>{employeesToRepeat}</b><small>сотрудников с ошибкой</small></article><article><span>Ещё не сдавали</span><b>{employeesNotStarted}</b><small>ни одной категории</small></article><article><span>Средний балл</span><b>{averageScore}%</b><small>по всем попыткам</small></article></div><div className="manager-tabs" role="tablist"><button className={managerTab === 'summary' ? 'active' : ''} onClick={() => setManagerTab('summary')}>Сводка команды</button><button className={managerTab === 'attempts' ? 'active' : ''} onClick={() => setManagerTab('attempts')}>Журнал попыток</button><button className={managerTab === 'team' ? 'active' : ''} onClick={() => setManagerTab('team')}>Сотрудники</button></div>{managerTab !== 'team' && <div className="manager-filters"><label><span>Сотрудник</span><input value={employeeFilter} onChange={(event) => setEmployeeFilter(event.target.value)} placeholder="Поиск по ФИО" /></label><label><span>Категория</span><select value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)}><option value="all">Все категории</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.title}</option>)}</select></label><label><span>Статус</span><select value={statusFilter} onChange={(event) => setStatusFilter(event.target.value as 'all' | ProgressStatus)}><option value="all">Все статусы</option><option value="passed">Сдано</option><option value="failed">Нужна пересдача</option>{managerTab === 'summary' && <option value="missing">Не сдавали</option>}</select></label>{managerTab === 'attempts' && <label><span>Дата попытки</span><input type="date" value={dateFilter} onChange={(event) => setDateFilter(event.target.value)} /></label>}</div>}{managerTab === 'summary' && <><div className="manager-section-heading"><div><h2>Матрица прохождения</h2><p>Нажмите на сотрудника или результат, чтобы открыть его карточку.</p></div><span>{filteredTeam.length} из {teamMembers.length}</span></div>{teamMembers.length === 0 ? <div className="manager-empty"><b>Сначала добавьте команду</b><p>Перейдите во вкладку «Сотрудники» и вставьте список ФИО — тогда появятся статусы «не сдавали» и «нужна пересдача».</p><button className="secondary-button" onClick={() => setManagerTab('team')}>Добавить сотрудников</button></div> : <div className="matrix-table"><div className="matrix-row matrix-head" style={{ gridTemplateColumns: `minmax(190px, 1.5fr) repeat(${shownCategories.length}, minmax(112px, 1fr))` }}><span>Сотрудник</span>{shownCategories.map((category) => <span key={category.id}>{category.title}</span>)}</div>{filteredTeam.length === 0 ? <p className="empty-attempts">По текущему фильтру сотрудников не найдено.</p> : filteredTeam.map((member) => <div className="matrix-row" key={member.id} style={{ gridTemplateColumns: `minmax(190px, 1.5fr) repeat(${shownCategories.length}, minmax(112px, 1fr))` }}><button className="member-name" onClick={() => setSelectedEmployee(member.name)}>{member.name}</button>{shownCategories.map((category) => { const attempt = latestByEmployeeCategory.get(attemptKey(member.name, category.id)); const status = currentStatus(member.name, category.id); return <button className={`matrix-cell ${status}`} key={category.id} onClick={() => setSelectedEmployee(member.name)}><b>{attempt ? `${attempt.score} / ${attempt.total}` : '—'}</b><small>{status === 'passed' ? 'Зачёт' : status === 'failed' ? 'Повторить' : 'Не сдавал'}</small></button>; })}</div>)}</div>}{selectedEmployee && <section className="employee-detail"><div className="employee-detail-top"><div><p className="eyebrow">КАРТОЧКА СОТРУДНИКА</p><h2>{selectedEmployee}</h2></div><button className="close-detail" onClick={() => setSelectedEmployee(null)}>Закрыть ×</button></div><div className="detail-summary"><article><span>Всего попыток</span><b>{selectedEmployeeAttempts.length}</b></article><article><span>Последний результат</span><b>{selectedEmployeeAttempts[0] ? `${selectedEmployeeAttempts[0].score} / ${selectedEmployeeAttempts[0].total}` : '—'}</b></article><article><span>Лучший результат</span><b>{selectedEmployeeBest ? `${selectedEmployeeBest.score} / ${selectedEmployeeBest.total}` : '—'}</b></article></div><div className="detail-status-grid">{categories.map((category) => { const attempt = latestByEmployeeCategory.get(attemptKey(selectedEmployee, category.id)); const status = currentStatus(selectedEmployee, category.id); return <button key={category.id} className={`detail-status ${status}`} onClick={() => setCategoryFilter(category.id)}><span>{category.title}</span><b>{attempt ? `${attempt.score} / ${attempt.total}` : 'Не сдавал'}</b><small>{status === 'passed' ? 'Зачёт' : status === 'failed' ? 'Нужна пересдача' : 'Нет попытки'}</small></button>; })}</div><div className="detail-attempts"><h3>Все попытки</h3>{selectedEmployeeAttempts.length === 0 ? <p>Сотрудник ещё не проходил тесты.</p> : selectedEmployeeAttempts.map((attempt) => <div key={attempt.id}><span>{attempt.categoryTitle}</span><span>{new Intl.DateTimeFormat('ru-RU', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }).format(new Date(attempt.completedAt))}</span><b>{attempt.score} / {attempt.total}</b><i className={attempt.passed ? 'status-pass' : 'status-repeat'}>{attempt.passed ? 'Зачёт' : 'Повторить'}</i></div>)}</div></section>}</>}{managerTab === 'attempts' && <><div className="manager-section-heading"><div><h2>Журнал попыток</h2><p>Показывает каждую сдачу; выгрузка учитывает текущие фильтры.</p></div><button className="secondary-button" onClick={exportAttempts}>Скачать CSV</button></div><div className="attempt-table"><div className="attempt-table-head"><span>Сотрудник</span><span>Категория</span><span>Дата</span><span>Результат</span><span>Статус</span></div>{filteredAttempts.length === 0 ? <p className="empty-attempts">Попыток по этому фильтру пока нет.</p> : filteredAttempts.map((attempt) => <div className="attempt-row" key={attempt.id}><button className="attempt-member" onClick={() => { setSelectedEmployee(attempt.employeeName); setManagerTab('summary'); }}>{attempt.employeeName}</button><span>{attempt.categoryTitle}</span><span>{new Intl.DateTimeFormat('ru-RU', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }).format(new Date(attempt.completedAt))}</span><strong>{attempt.score} / {attempt.total}</strong><i className={attempt.passed ? 'status-pass' : 'status-repeat'}>{attempt.passed ? 'Зачёт' : 'Повторить'}</i></div>)}</div></>}{managerTab === 'team' && <><div className="manager-section-heading"><div><h2>Реестр команды</h2><p>Добавьте сотрудников один раз — сводка сразу покажет, кто не начал или кому нужна пересдача.</p></div><span>{teamMembers.length} сотрудников</span></div><form className="team-import" onSubmit={(event) => void saveTeamMembers(event)}><label htmlFor="team-members">Список ФИО</label><textarea id="team-members" value={teamDraft} onChange={(event) => { setTeamDraft(event.target.value); setTeamActionState('idle'); }} placeholder={'Одно ФИО на строку\nАнна Соколова\nИван Петров'} /><div><small>Можно вставить до 100 имён за раз: по одному в строке, через запятую или точку с запятой.</small><button className="primary-button" type="submit" disabled={!teamDraft.trim() || teamActionState === 'saving'}>{teamActionState === 'saving' ? 'Сохраняем…' : 'Добавить в реестр'} <span>→</span></button></div>{teamActionState === 'error' && <p className="team-error">Не удалось сохранить список. Попробуйте ещё раз.</p>}</form><div className="team-list">{teamMembers.length === 0 ? <p className="empty-attempts">Реестр пока пуст.</p> : teamMembers.map((member) => <div key={member.id}><b>{member.name}</b><button onClick={() => void removeTeamMember(member)} aria-label={`Убрать ${member.name} из списка`}>Убрать</button></div>)}</div></>}</section>}

    {screen === 'myResults' && <section className="dashboard-view"><div className="dashboard-top"><div><button className="back-button" onClick={() => setScreen('categories')}>← К категориям</button><p className="eyebrow">ЛИЧНЫЙ РЕЗУЛЬТАТ</p><h1>Мои попытки</h1></div><button className="secondary-button" onClick={() => void loadMyResults()}>{journalLoading ? 'Обновляем…' : 'Обновить'}</button></div><div className="stat-grid"><article><span>Всего попыток</span><b>{attempts.length}</b></article><article><span>Зачётов</span><b>{attempts.filter((attempt) => attempt.passed).length}</b></article><article><span>Средний результат</span><b>{averageScore}%</b></article></div><div className="attempt-table"><div className="attempt-table-head personal-attempt-head"><span>Категория</span><span>Дата</span><span>Результат</span><span>Статус</span></div>{attempts.length === 0 ? <p className="empty-attempts">У вас пока нет сохранённых попыток.</p> : attempts.map((attempt) => <div className="attempt-row personal-attempt-row" key={attempt.id}><b>{attempt.categoryTitle}</b><span>{new Intl.DateTimeFormat('ru-RU', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }).format(new Date(attempt.completedAt))}</span><strong>{attempt.score} / {attempt.total}</strong><i className={attempt.passed ? 'status-pass' : 'status-repeat'}>{attempt.passed ? 'Зачёт' : 'Повторить'}</i></div>)}</div></section>}

    {screen === 'quiz' && activeCategory && questions[questionIndex] && <section className="quiz-view"><div className="quiz-topline"><button className="back-button" onClick={() => setScreen('categories')}>← К категориям</button><span>{activeCategory.title} · вопрос {questionIndex + 1} из {questions.length}</span></div><div className="progress-track" aria-label={`Выполнено ${questionIndex + 1} из ${questions.length}`}><i style={{ width: `${((questionIndex + 1) / questions.length) * 100}%` }} /></div><article className="question-card"><span className="question-number">{String(questionIndex + 1).padStart(2, '0')}</span><p className="question-kicker">ВЫБЕРИТЕ ОДИН ВАРИАНТ</p><h1>{questions[questionIndex].text}</h1><div className="answer-list">{questions[questionIndex].options.map((option, index) => <button key={option} className={`answer-option ${selected === index ? 'selected' : ''}`} onClick={() => setSelected(index)} aria-pressed={selected === index}><span>{String.fromCharCode(65 + index)}</span><p>{option}</p><i aria-hidden="true" /></button>)}</div><div className="question-actions"><span>{selected === null ? 'Выберите вариант ответа' : 'Ответ выбран'}</span><button className="primary-button" onClick={nextQuestion} disabled={selected === null}>{questionIndex + 1 === questions.length ? 'Завершить тест' : 'Следующий вопрос'} <b>→</b></button></div></article></section>}

    {screen === 'result' && activeCategory && <section className="result-view"><div className={`result-hero ${passed ? 'passed' : 'not-passed'}`}><div className="score-orbit" style={{ '--score': `${(score / questions.length) * 100}%` } as React.CSSProperties}><div><b>{score}</b><span>из {questions.length}</span></div></div><div><p className="eyebrow">{activeCategory.title.toUpperCase()} · РЕЗУЛЬТАТ</p><h1>{passed ? 'Зачёт. Отличная работа!' : 'Есть что повторить.'}</h1><p>{passed ? `Вы набрали ${score} баллов при проходном результате ${currentPassScore}.` : `Для зачёта нужно ${currentPassScore} балл. Сейчас — ${score}. Разберите ошибки и попробуйте снова.`}</p><div className="result-actions"><button className="primary-button" onClick={retry}>Пройти ещё раз <b>↻</b></button><button className="secondary-button" onClick={() => setScreen('categories')}>Другая категория</button></div><small className={`save-state ${saveState}`}>{saveState === 'saving' && 'Сохраняем результат…'}{saveState === 'saved' && '✓ Результат сохранён в журнале'}{saveState === 'error' && 'Результат не сохранился — повторите попытку позже.'}</small></div></div><div className="review-heading"><div><p className="eyebrow">РАЗБОР ОТВЕТОВ</p><h2>{score === questions.length ? 'Все ответы верные' : `Ошибок: ${questions.length - score}`}</h2></div><span>{score === questions.length ? 'Можно переходить к следующей категории.' : 'Откройте карточки ниже — там верный ответ и пояснение.'}</span></div>{score !== questions.length && <div className="mistake-list">{questions.map((question, index) => answers[index] !== question.correct && <details key={question.id} className="mistake-card" open><summary><span>Вопрос {question.id}</span><p>{question.text}</p><b>⌄</b></summary><div className="mistake-body"><p className="your-answer"><small>Ваш ответ</small>{question.options[answers[index]]}</p><p className="right-answer"><small>Верный ответ</small>{question.options[question.correct]}</p><p className="standard-note">{question.note}</p></div></details>)}</div>}</section>}
  </main>;
}
